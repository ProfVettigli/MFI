# MFI — Istruzioni per Claude

## Workflow Git

- Committa **sempre direttamente su `main`** (non creare branch di feature).
- Dopo ogni commit fai `git push -u origin main` così il deploy su GitHub Pages avviene automaticamente.
- Non aprire Pull Request a meno che l'utente lo richieda esplicitamente.
